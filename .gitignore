node_modules/
.next/
.dev/
# Uploaded and generated files, on the DEV driver (lib/media/local.ts).
# Online these live in a bucket — see .env.example.
.data/
dist/
out/
# `npm run test:coverage` — a tool, run on purpose, never committed.
coverage/
*.log

.env
.env.local
.env.*.local

.DS_Store
next-env.d.ts
*.tsbuildinfo

# Agent working artifacts — Playwright MCP output and session screenshots
# dropped in the repo root. Real app images live in public/ and .data/;
# the leading slash keeps these patterns from reaching either.
.playwright-mcp/
/*.png
/*.jpg
/*.jpeg
